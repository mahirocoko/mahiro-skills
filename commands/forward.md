---
description: v1.6.0 G-CMD | Create handoff + enter planning flow for next session. Use when user says "forward", "handoff", "wrap up", or before ending session.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /forward

Execute the `forward` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "forward"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/forward/SKILL.md` and follow ALL instructions in it.
