---
description: Day-to-day CocoIndex Code search and index lifecycle with portable project settings, filename-only preflight, one pinned Gitleaks self-heal, and strict receipt checks.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
---

# /ccc

Execute the `ccc` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "ccc"` instead of reading the file manually.

**Otherwise**: Resolve the installed `ccc/SKILL.md` under the current agent's configured skills root, then follow ALL instructions in it. Do not assume a source-checkout-relative `skills/...` path.
