---
description: Create an explicit repo-grounded frontend design brief, reference breakdown, redesign plan, or reference-to-implementation fidelity contract. Do not auto-load for native model-taste baselines.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /frontend-design

Execute the `frontend-design` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "frontend-design"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/frontend-design/SKILL.md` and follow ALL instructions in it.
