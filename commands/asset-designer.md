---
description: Directs website image assets and Agy/Gemini-first dicut work with an explicit Codex fallback. Use when users want to cut out/dicut images, remove backgrounds, separate layers, clean edges, create transparent PNG assets, generate missing web assets, or prepare production-ready asset packs for UI/web projects.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /asset-designer

Execute the `asset-designer` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "asset-designer"` instead of reading the file manually.

**Otherwise**: Resolve the installed `asset-designer/SKILL.md` under the current agent's configured skills root, then follow ALL instructions in it. Do not assume a source-checkout-relative `skills/...` path.
