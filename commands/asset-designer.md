---
description: Personal designer workflow for website image assets. Use when users want to cut out/dicut images, remove backgrounds, separate layers, clean edges, create transparent PNG assets, generate missing web assets, or prepare production-ready asset packs for UI/web projects.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /asset-designer

Execute the `asset-designer` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "asset-designer"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/asset-designer/SKILL.md` and follow ALL instructions in it.
