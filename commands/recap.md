---
description: Session orientation and awareness. Use when starting work, resuming context, or answering status questions.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /recap

Execute the `recap` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "recap"` instead of reading the file manually.

**Otherwise**: Resolve the installed `recap/SKILL.md` under the current agent's configured skills root, then follow ALL instructions in it. Do not assume a source-checkout-relative `skills/...` path.
