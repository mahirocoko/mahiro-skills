---
description: Repo-reality-first AGENTS.md and docs-family bootstrapper. Uses reference grammar for shape and layers Mahiro-style only as preferred direction or fallback when the target repo is silent, partial, or ambiguous.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /mahiro-docs-rules-init

Execute the `mahiro-docs-rules-init` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "mahiro-docs-rules-init"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/mahiro-docs-rules-init/SKILL.md` and follow ALL instructions in it.
