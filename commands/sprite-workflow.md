---
description: CLI-first sprite production workflow for prompts, imagegen handoffs, motion references, extraction, runtime QA, native-grid recovery, atlases, previews, and promotion gates.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /sprite-workflow

Execute the `sprite-workflow` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "sprite-workflow"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/sprite-workflow/SKILL.md` and follow ALL instructions in it.
