---
description: v1.6.0 G-CMD | Display the local philosophy — the 5 Principles + Rule 6. Use when user asks about principles, "nothing deleted", "patterns over intentions", or needs alignment checks. Do NOT trigger for identity or session status questions.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /philosophy

Execute the `philosophy` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "philosophy"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/philosophy/SKILL.md` and follow ALL instructions in it.
