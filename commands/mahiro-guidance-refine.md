---
description: Refine repo docs, AGENTS.md, and Mahiro-style guidance from session feedback. Use when the user corrects agent behavior, identifies preferred patterns, or asks to update rules from lessons learned.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /mahiro-guidance-refine

Execute the `mahiro-guidance-refine` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "mahiro-guidance-refine"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/mahiro-guidance-refine/SKILL.md` and follow ALL instructions in it.
