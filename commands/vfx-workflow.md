---
description: Direct production runtime VFX truth, geometry, cue timing, accessibility, pooling, performance, QA, and promotion.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /vfx-workflow

Execute the `vfx-workflow` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "vfx-workflow"` instead of reading the file manually.

**Otherwise**: Resolve the installed `vfx-workflow/SKILL.md` under the current agent's configured skills root, then follow ALL instructions in it. Do not assume a source-checkout-relative `skills/...` path.
