---
description: Orchestrate hard, ambiguous, long-running, or repeatedly failing work through bounded hypotheses, adaptive specialist lanes, checkpoints, and fresh verification. This is a workflow mode, not Cursor Fable model selection.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /fable

Execute the `fable` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "fable"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/fable/SKILL.md` and follow ALL instructions in it.
