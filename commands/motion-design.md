---
description: Design or audit an explicit UI/product motion system, choreography, timing, easing, spring, stagger, interaction-state motion, and reduced-motion behavior without auto-routing ordinary frontend work.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
---

# /motion-design

Execute the `motion-design` skill with args: `$ARGUMENTS`

Use only for an explicitly named motion-design brief, system, choreography, interaction-state motion, or rendered motion audit. Do not auto-route ordinary frontend implementation or game VFX here.

**If you have a Skill tool available**: Use it directly with `skill: "motion-design"` instead of reading the file manually.

**Otherwise**: Resolve the installed `motion-design/SKILL.md` under the current agent's configured skills root, then follow ALL instructions in it. Do not assume a source-checkout-relative `skills/...` path.
