---
description: Drafts, applies, or refines Goal Mode objectives, Definition of Done (DoD), immediate next steps, verification evidence, and handoff/reset wording. Use when the user mentions /goal, goal mode, create goal, DoD, next step, verified, safe, handoff, reset, or asks to structure work before execution.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /control-room-goals (Goal Mode)

Execute the `control-room-goals` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "control-room-goals"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/control-room-goals/SKILL.md` and follow ALL instructions in it.
