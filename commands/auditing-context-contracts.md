---
description: Audit repository text/context contracts for stale API, route, package, capability, ownership, generated-output, comment, agent-state, and historical claims without treating keyword scans as semantic proof.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
---

# /auditing-context-contracts

Execute the `auditing-context-contracts` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "auditing-context-contracts"` instead of reading the file manually.

**Otherwise**: Read the skill file at `skills/auditing-context-contracts/SKILL.md` and follow ALL instructions in it.

