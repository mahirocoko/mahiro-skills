---
description: Direct whole-game production maturity, inventories, specialist lanes, QA, performance, and release readiness.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /game-production

Execute the `game-production` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "game-production"` instead of reading the file manually.

**Otherwise**: Resolve the installed `game-production/SKILL.md` under the current agent's configured skills root, then follow ALL instructions in it. Do not assume a source-checkout-relative `skills/...` path.
