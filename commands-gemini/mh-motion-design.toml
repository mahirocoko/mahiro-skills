description = "Design or audit an explicit UI/product motion system, choreography, timing, easing, spring, stagger, interaction-state motion, and reduced-motion behavior. Do not auto-route ordinary frontend work or game VFX here."
prompt = """
Execute the `motion-design` skill with args: {{args}}

Use only for an explicitly named motion-design brief, system, choreography, interaction-state motion, or rendered motion audit. Do not auto-route ordinary frontend implementation or game VFX here.

If you have a Skill tool available: Use it directly with `skill: \"motion-design\"` instead of reading the file manually.

Otherwise: Read the installed skill file from the Gemini skills root (`.gemini/skills/motion-design/SKILL.md` for local installs or `~/.gemini/skills/motion-design/SKILL.md` for global installs) and follow ALL instructions in it.
"""
