description = "CLI-first sprite/Codex handoff workflow for sprite sheets, mascot frames, contact sheets, preview GIFs, QA, and promotion gates."
prompt = """
Execute the `sprite-workflow` skill with args: {{args}}

If you have a Skill tool available: Use it directly with `skill: \"sprite-workflow\"` instead of reading the file manually.

Otherwise: Read the installed skill file from the Gemini skills root (`.gemini/skills/sprite-workflow/SKILL.md` for local installs or `~/.gemini/skills/sprite-workflow/SKILL.md` for global installs) and follow ALL instructions in it.
"""
