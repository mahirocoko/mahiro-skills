description = "Learn from YouTube videos via Gemini transcription. Use when the user wants to study a video or transcript a YouTube link."
prompt = """
Execute the `watch` skill with args: {{args}}

If you have a Skill tool available: Use it directly with `skill: \"watch\"` instead of reading the file manually.

Otherwise: Read the installed skill file from the Gemini skills root (`.gemini/skills/watch/SKILL.md` for local installs or `~/.gemini/skills/watch/SKILL.md` for global installs) and follow ALL instructions in it.
"""
